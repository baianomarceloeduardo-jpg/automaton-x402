{
  "$schema": "https://raw.githubusercontent.com/modelcontextprotocol/registry/main/schema/server.json",
  "name": "automaton-x402-trust",
  "title": "Automaton x402 Trust & Security Layer",
  "description": "Autonomous security scanner, on-chain oracle verification, and x402 compliance linter for AI agent services on Base L2",
  "version": "2.0.0",
  "license": "MIT",
  "author": {
    "name": "Automaton-Sovereign",
    "url": "https://hardly-animals-cyber-theatre.trycloudflare.com"
  },
  "homepage": "https://hardly-animals-cyber-theatre.trycloudflare.com",
  "repository": {
    "type": "git",
    "url": "https://github.com/automaton-sovereign/automaton-x402"
  },
  "capabilities": {
    "tools": [
      {
        "name": "check_x402_conformance",
        "description": "Audit any AI agent micropayment service against the official Coinbase x402 v1/v2 and EIP-3009 standard.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "url": {
              "type": "string",
              "description": "URL of the target API endpoint"
            }
          },
          "required": ["url"]
        }
      },
      {
        "name": "scan_token_security",
        "description": "Static EVM bytecode decompilation and honeypot risk scan for Base ERC-20 tokens.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "address": {
              "type": "string",
              "description": "Base token contract address"
            }
          },
          "required": ["address"]
        }
      },
      {
        "name": "get_onchain_oracle_price",
        "description": "Fetch real Uniswap V3 TWAP price and Chainlink reference deviation for Base assets (zero gas).",
        "inputSchema": {
          "type": "object",
          "properties": {
            "asset": {
              "type": "string",
              "enum": ["ETH", "cbBTC", "AERO"],
              "description": "Asset symbol"
            }
          },
          "required": ["asset"]
        }
      }
    ]
  }
}
